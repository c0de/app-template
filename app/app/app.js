var app,
  services    = angular.module('veApp.services',    []),
  directives  = angular.module('veApp.directives',  []),
  filters     = angular.module('veApp.filters',     []),
  controllers = angular.module('veApp.controllers', []);

app = angular.module('myApp', []);

app.config(function() {
});

app.run(function() {
});
